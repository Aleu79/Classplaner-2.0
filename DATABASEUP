-- phpMyAdmin SQL Dump
-- version 5.2.3
-- https://www.phpmyadmin.net/
--
-- Host: localhost
-- Generation Time: Oct 29, 2025 at 07:51 PM
-- Server version: 10.5.29-MariaDB-0+deb11u1
-- PHP Version: 8.3.27

SET SQL_MODE = "NO_AUTO_VALUE_ON_ZERO";
START TRANSACTION;
SET time_zone = "+00:00";


/*!40101 SET @OLD_CHARACTER_SET_CLIENT=@@CHARACTER_SET_CLIENT */;
/*!40101 SET @OLD_CHARACTER_SET_RESULTS=@@CHARACTER_SET_RESULTS */;
/*!40101 SET @OLD_COLLATION_CONNECTION=@@COLLATION_CONNECTION */;
/*!40101 SET NAMES utf8mb4 */;

--
-- Database: `classplanner`
--

-- --------------------------------------------------------

--
-- Table structure for table `califications`
--

CREATE TABLE califications (
  id_calification SERIAL PRIMARY KEY,
  id_task INTEGER DEFAULT NULL,
  id_comment INTEGER DEFAULT NULL,
  calification INTEGER DEFAULT NULL
);

-- --------------------------------------------------------

--
-- Table structure for table `classes`
--

CREATE TABLE classes (
  id_class SERIAL PRIMARY KEY,
  class_name VARCHAR(255) DEFAULT NULL,
  class_profesor INTEGER DEFAULT NULL,
  class_curso VARCHAR(50) DEFAULT NULL,
  class_color VARCHAR(50) DEFAULT NULL,
  class_token VARCHAR(10) NOT NULL UNIQUE
);

--
-- Dumping data for table `classes`
--

-- --------------------------------------------------------

--
-- Table structure for table `class_users`
--

CREATE TABLE class_users (
  id SERIAL PRIMARY KEY,
  id_user INTEGER DEFAULT NULL,
  id_class INTEGER DEFAULT NULL
);

--
-- Dumping data for table `class_users`
--


-- --------------------------------------------------------

--
-- Table structure for table `comments`
--

CREATE TABLE comments (
  id_comment SERIAL PRIMARY KEY,
  id_user INTEGER DEFAULT NULL,
  id_task INTEGER DEFAULT NULL,
  comment TEXT DEFAULT NULL,
  created_on TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

--
-- Dumping data for table `comments`
--


-- --------------------------------------------------------

--
-- Table structure for table `submissions`
--

CREATE TABLE submissions (
  id_submission SERIAL PRIMARY KEY,
  id_user INTEGER NOT NULL,
  id_task INTEGER NOT NULL,
  submission_file VARCHAR(200) NOT NULL,
  submission_comment VARCHAR(500) NOT NULL,
  submission_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  calification INTEGER DEFAULT NULL,
  feedback VARCHAR(500) DEFAULT NULL
);

-- --------------------------------------------------------

--
-- Table structure for table `tasks`
--

CREATE TABLE tasks (
  id_task SERIAL PRIMARY KEY,
  id_class INTEGER DEFAULT NULL,
  title VARCHAR(255) DEFAULT NULL,
  description TEXT DEFAULT NULL,
  created_on TIMESTAMP DEFAULT CURRENT_DATE,
  deliver_until TIMESTAMP DEFAULT NULL
);

--
-- Dumping data for table `tasks`

-- --------------------------------------------------------

--
-- Table structure for table `users`
--

CREATE TABLE users (
  id_user SERIAL PRIMARY KEY,
  user_name VARCHAR(30) NOT NULL,
  user_lastname VARCHAR(20) NOT NULL,
  user_password VARCHAR(100) NOT NULL,
  user_mail VARCHAR(100) NOT NULL UNIQUE,
  user_type VARCHAR(20) NOT NULL,
  user_alias VARCHAR(20) NOT NULL,
  user_photo VARCHAR(200) DEFAULT NULL
);

--
-- Dumping data for table `users`
--

--
-- Constraints for dumped tables
--

--
-- Constraints for table `califications`
--
ALTER TABLE califications
  ADD CONSTRAINT califications_ibfk_1 FOREIGN KEY (id_task) REFERENCES tasks (id_task),
  ADD CONSTRAINT califications_ibfk_2 FOREIGN KEY (id_comment) REFERENCES comments (id_comment);

--
-- Constraints for table `classes`
--
ALTER TABLE classes
  ADD CONSTRAINT classes_ibfk_1 FOREIGN KEY (class_profesor) REFERENCES users (id_user);

--
-- Constraints for table `class_users`
--
ALTER TABLE class_users
  ADD CONSTRAINT class_users_ibfk_1 FOREIGN KEY (id_user) REFERENCES users (id_user),
  ADD CONSTRAINT class_users_ibfk_2 FOREIGN KEY (id_class) REFERENCES classes (id_class);

--
-- Constraints for table `comments`
--
ALTER TABLE comments
  ADD CONSTRAINT comments_ibfk_1 FOREIGN KEY (id_user) REFERENCES users (id_user),
  ADD CONSTRAINT comments_ibfk_2 FOREIGN KEY (id_task) REFERENCES tasks (id_task);

--
-- Constraints for table `submissions`
--
ALTER TABLE submissions
  ADD CONSTRAINT submissions_ibfk_1 FOREIGN KEY (id_user) REFERENCES users (id_user) ON DELETE CASCADE ON UPDATE CASCADE,
  ADD CONSTRAINT submissions_ibfk_2 FOREIGN KEY (id_task) REFERENCES tasks (id_task) ON DELETE CASCADE ON UPDATE CASCADE;

--
-- Constraints for table `tasks`
--
ALTER TABLE tasks
  ADD CONSTRAINT tasks_ibfk_1 FOREIGN KEY (id_class) REFERENCES classes (id_class);

/*!40101 SET CHARACTER_SET_CLIENT=@OLD_CHARACTER_SET_CLIENT */;
/*!40101 SET CHARACTER_SET_RESULTS=@OLD_CHARACTER_SET_RESULTS */;
/*!40101 SET COLLATION_CONNECTION=@OLD_COLLATION_CONNECTION */;